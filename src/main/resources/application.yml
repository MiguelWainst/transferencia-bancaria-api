spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/transferencia_bancaria
    username: miguel
    password: "123"
    hikari:
      maximum-pool-size: 50
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080